function grep --description 'alias grep grep -I --color=auto --exclude-dir=Steam --exclude-dir=overlay --exclude-dir=cache'
    command grep -I --color=auto --exclude-dir=Steam --exclude-dir=overlay --exclude-dir=cache $argv
end
